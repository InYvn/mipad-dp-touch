# 安装与使用

把小米平板 9 Pro Max 上的小米焦点触控笔 Pro 变成 Mac 上真正可用的输入设备。

用一根 USB-C 线把小米平板（DP-in 模式当外接屏）连到 Mac，平板的笔就能：
- **轻点** = 鼠标点击
- **按住划动** = 滚动翻页（默认）或拖拽 / 划选文字

安装后常驻菜单栏，没有 Dock 图标，不需要终端、不需要装 Python。

---

## 系统要求

| 项目 | 要求 |
|---|---|
| Mac | **macOS 27**（开发与实测环境；老系统有回溯分支，但未验证） |
| 平板 | 小米平板 9 Pro Max（已实测）。其它小米 DP-in 平板见下文「设备支持」 |
| 连线 | 一根**全功能** USB-C 转 USB-C 线，直连 Mac 的雷雳口 |
| 权限 | 「输入监控」+「辅助功能」（下面会引导你开） |

> 平板必须在 **DP-in / 外接显示器模式**下连接（Mac 认到它是一块外接屏）。

---

## 安装

1. 从 Releases 下载 **`Mipad-DP-Touch-1.0.0.dmg`**，双击打开。
2. 把里面的 **Mipad DP Touch** 拖到旁边的 **「应用程序」** —— 拖过去就装好了。
3. 从「应用程序」里双击打开。

> **如果提示「无法打开，因为来自身份不明的开发者」或「已损坏」**：这是没有 Apple 公证的正常现象，
> **不是文件坏了**。任选一种放行（只需一次）：
> 打开「系统设置 → 隐私与安全性」，在「安全性」一栏点被拦那条的 **「仍要打开」**；
> 或者在终端运行 `xattr -dr com.apple.quarantine "/Applications/Mipad DP Touch.app"`。

打开后菜单栏会出现一个**笔形图标**，`Dock` 里没有图标 —— 这是正常的。

---

## 首次运行：分步授权（只做一次）

macOS 不允许程序自己给自己授权，所以必须你在系统设置里点两下。
App **不会一次把两个系统弹窗糊你脸上** —— 它会打开自己的设置窗口，一步一步来：

```
权限                                        0 / 2 已完成
  1. 辅助功能   允许合成点击与拖拽，否则笔点了没反应          [打开设置]
  2. 输入监控   读取平板笔的原始数据，否则收不到任何笔事件     [打开设置]   ← 灰的
```

1. 点第 1 步的 **打开设置** → 在 `系统设置 → 隐私与安全性 → 辅助功能` 里打开
   **Mipad DP Touch** 的开关。
   > **macOS 27 注意**：新版系统把它归到了
   > `系统设置 → 隐私与安全性 → 设备控制和数据访问`，两个位置都找一下，哪个有开哪个。
2. 回到窗口：**第 1 步变成 ✓、第 2 步自己亮起来**（窗口每秒对一次权限状态）。
   再点第 2 步的 **打开设置** → 在 `系统设置 → 隐私与安全性 → 输入监控` 里打开开关。
3. 两项都 ✓ 之后，窗口会出现 **立即重启**：macOS 的权限判定发生在**进程启动时**，
   授权后必须让 app 重新建立连接才生效。点它就重启，不用手动退出再打开。
   （不想重启就点旁边的 **重新检测**，它会重新连一次并如实告诉你成没成。）

> 如果系统设置列表里找不到这个 app：点对应那一步的「打开设置」把面板打开一次，
> macOS 通常会把当前 app 自动加进列表；实在没有就用列表下方的 `+` 从「应用程序」里添加。

> 首启引导**只自动弹一次**（配置里记了 `onboarded`）—— 否则开了「登录时自动启动」之后
> 每次登录都弹窗口会很烦。以后想再看：菜单栏 → **完成授权…**（权限不齐时才有）或 **设置…**。

### 4. 更新 App 之后权限失效？（ad-hoc 签名 + TCC 的老问题）

如果你重新打包、或下载了新版覆盖 `/Applications` 里的 App，之后出现
**「开关明明是开着的，App 却还说未授权」**——这是 TCC 的已知行为，不是你操作错了。

原因：本 App 是 **ad-hoc 签名**（免费，不需要开发者证书）。macOS 对这类 App 按
**代码哈希（cdhash）** 记身份，重新打包后哈希必变，系统里那条旧授权就对不上了。
系统日志里的证据长这样：

```
tccd: Failed to match existing code requirement for subject io.github.inyvn.xiaomi-dptouch
      and service kTCCServiceAccessibility
```

**本项目已经处理过**：`build_app.sh` 在 ad-hoc 签名之外，再给 App 补一条
「稳定指定要求」`designated => identifier "io.github.inyvn.xiaomi-dptouch"`
（只认 bundle id、不认哈希），所以授权能跨重新打包保留。

> 万一还是碰上（例如装的是加固前的旧版），重置一次再授权即可：
>
> ```bash
> tccutil reset Accessibility io.github.inyvn.xiaomi-dptouch
> tccutil reset ListenEvent   io.github.inyvn.xiaomi-dptouch
> ```
>
> 然后重启 App、重新授权。注意：**光把开关关掉再打开是没用的**，那条记录绑的是
> 上个版本的哈希，必须整条重置掉。

> 顺带一个给开发者的坑：这条自定义要求**不能和 `codesign --deep` 连用**。
> `--deep` 会把 `identifier "io.github.inyvn.xiaomi-dptouch"` 这条要求套到每个
> 嵌套的 `.so` 上（它们的 bundle id 根本不是这个），结果是
> `nested code is modified or invalid`，实测 20 个文件校验失败。
> 正确顺序是：先 `--deep` 签嵌套，**再单独**给外层补 `-r`。

---

## 界面说明

**菜单栏**（短菜单，只放常用动作）：

```
● 已连接 · 小米平板 9 Pro Max · 34 滚动 / 12 点击    ← 实时状态
  完成授权…                                        ← 只在权限不齐时出现
☑ 启用笔桥接                                        ← 临时停用（不用拔线）
  清零计数
──────────────────────────
  显示缩放 ▸  1704×1136 / 1280×853 / …             ← 只动平板那块屏，见下文「显示」
              退回切换前的档位 / 重新扫描显示器 / 打开「显示器」设置…
──────────────────────────
  设置…  (⌘,)                                      ← 所有设置都在这里
  打开日志
──────────────────────────
  退出 Mipad DP Touch  (⌘Q)
```

**设置窗口**（菜单「设置…」或 ⌘,；620×1026 固定尺寸，不可缩放）：

```
状态
  ● 已连接 · 小米平板 9 Pro Max
  笔事件 1284 · 点击 12 · 滚动 34 · 拖拽 0

权限                                            0 / 2 已完成
  1. 辅助功能   允许合成点击与拖拽，否则笔点了没反应        [打开设置]
  2. 输入监控   读取平板笔的原始数据，否则收不到任何笔事件   [打开设置]
     改动需要重启本程序才生效                    [重新检测][立即重启]

笔的输入
  滑动方式 [滑动翻页 · 不选中文字 ▾]        ← 默认：看文档、翻网页
  ☑ 停住再划 = 拖拽    [0.25s ▾]           ← 翻页模式下的第二个手势
  长按不动   [0.8 秒 ▾]  弹出右键菜单        ← 停住不动再抬手 = 右键
  滚动方向 [跟随系统 ▾]   系统当前：传统     ← 跟着你 Mac 的滚动设置走
  滚动速度 [1x 标准 ▾]

桥接
  ☑ 启用笔桥接          ☐ 用笔的绝对坐标驱动光标（试验）
  ☐ 允许未验证的小米设备                   ← 给未来机型的兜底开关

显示缩放                                    只改平板那块屏，其他屏不动
  缩放档位 [1704×1136 · 像素 1:1 最锐 · 120Hz ▾]        [重新扫描]
  当前 UI 1704×1136 · 帧缓冲 3408×2272 · 120Hz · HiDPI
  ☐ 允许管理未实测的显示器

启动与日志
  ☑ 登录时自动启动        已登记为登录项
  ☐ 详细日志                          [打开日志][诊断…]

Mipad DP Touch 1.0.0 · MIT License
```

全部设置自动保存在
`~/Library/Application Support/MipadDPTouch/config.json`，下次启动恢复。

### 关于「滚动方向」

Mac 的**系统设置 → 鼠标/触控板 → 自然滚动**决定了你习惯哪个方向。
app 默认**跟随这个设置**，你不需要理解原理。

如果觉得方向反了，直接选「始终自然」或「始终传统」覆盖即可。

### 关于「滑动方式」与选中文字

选「滑动翻页」时，笔尖划动**完全不会产生鼠标按键事件**，所以不可能误选中文字 ——
不是"划完再取消选择"，而是压根没有"按住"这个状态。

## 显示：字太小？用 HiDPI 缩放（不变糊）

平板面板原生 **3408×2272**（13.3"、308 ppi，屏宽约 11.07"）。

- 以 **1x** 送 3408×2272：逻辑像素铺满整屏 ≈ **308 逻辑 PPI**，字比 MacBook Air 13" 小 2.4 倍
  —— 这就是「字太小」那一档。
- 直接拉低分辨率（如 2560×1440）字会变大，但**帧缓冲小于面板原生，平板要自己放大** → 糊。

macOS 其实已经为这块屏生成好了 **HiDPI** 档（渲染按 2x 画，再 1:1 送给面板）：

| 档位 | UI 看起来 | 帧缓冲（真实信号） | 逻辑 PPI | 锐度 |
|---|---|---|---|---|
| **推荐** | 1704×1136 @120Hz | **3408×2272**（= 面板原生，1:1） | ≈154 | 最锐，零放大 |
| 字更大 | 1280×853 @120Hz | 2560×1706 | ≈116 | 被平板放大 1.33x，略软 |
| （原状） | 3408×2272 1x @120Hz | 3408×2272 | ≈308 | 1:1 但字极小 |

参考：MacBook Air 13.6" 默认 1470×956 ≈ 128 逻辑 PPI；「更多空间」1710×1112 ≈ 148 逻辑 PPI。

切换就在 **App 里做**（见下一节）。改档会**持久生效**，重启后仍然是这一档。

选择是**持久**的：系统会记住，重启后仍是这一档。

### 在 App 里切换（推荐，且多屏安全）

菜单栏 **显示缩放 ▸** 就是上面这些档，点一下就切：

- 列表**只针对平板那块屏**：先用 EDID（厂商 `25001` / 型号 `4`）+ 屏幕名认出平板，认不出就把
  整组变灰、什么都不做。Mac 自带屏**永不改**（改坏了自己那块屏就没法点菜单自救）。
- 多个屏都像平板 → **拒绝**（宁可不动也不猜）。
- 提交时只改目标屏的**模式**，**不碰其他屏的分辨率、排列、镜像** —— 其他屏完全不受影响。
- 切完**自动重建坐标基准**（`engine.resync_display()`，只换 Bridge 对象、不重开 HID），
  笔的落点立刻对齐新尺寸 —— App 内切换**不用重启**。

> 面板原生那档（1x 3408×2272）不在 CG API 的枚举里，要回那档只能用「打开「显示器」设置…」
> 在系统设置里选。

---

## 故障排除

| 现象 | 原因 / 处理 |
|---|---|
| 菜单显示 `未检测到设备` | 平板没接好，或不在 DP-in 模式。重新插线，看平板是否作为外接屏亮起 |
| 输入监控显示 `✗`，笔没反应 | 去 `隐私与安全性 → 输入监控` 打开它，然后点「重新检测权限」 |
| 开关是**开着的**，App 仍报未授权（重装/更新之后） | 那条授权绑的是上一个版本的代码哈希，关掉再打开没用。`tccutil reset Accessibility io.github.inyvn.xiaomi-dptouch` + `tccutil reset ListenEvent io.github.inyvn.xiaomi-dptouch`，再重启 App 重新授权。详见上面 §4 |
| 笔能移光标但点不中 | 辅助功能没开。同上处理 |
| 点击、滚动都正常但**手指**没反应 | **正常，且当前无解。** 见 [README](README.md#它解决什么问题)：平板对外接主机只上报笔这一路数据，触摸屏那一路压根不存在。这是设备固件的取舍，软件层绕不过去 |
| 想弹右键菜单 | 笔尖停在原地别动、约 0.8 秒后抬手（设置窗口「笔的输入」→「长按不动」，可改 0.6 / 1.0 秒，也能关掉）。停住之后又划走仍算拖拽，不会误弹 |
| 滚动方向反了 | 「滚动方向」里选另一个 |
| 滚动太快 / 太慢 | 「滚动速度」里调 |
| 字太小想放大又不想变糊 | 菜单 **显示缩放 ▸** 点「UI 1704×1136 (像素 1:1 最锐)」。**别直接降分辨率** |
| 多屏时「显示缩放」整组变灰 | 没认出平板那块屏（EDID/名字不匹配）→ 勾「允许管理未实测的屏」；这样设计是为了不误改别的屏 |
| 换过分辨率后笔的**落点偏了** | 用**菜单里的「显示缩放」**切会自动重算坐标基准，不用重启 |
| 什么都不对 | 用 ⌘, 打开设置窗口 →「启动与日志」→「诊断…」跑一遍自检并把结果写进日志；「打开日志」看 `~/Library/Logs/MipadDPTouch.log` |

### 无头自检（排障用，不进 GUI 交互）

输出都在 `~/Library/Logs/MipadDPTouch.log`：

```bash
APP="/Applications/Mipad DP Touch.app/Contents/MacOS/MipadDPTouch"

# 1) 打印真实菜单树 + 认到哪块屏 + 这块屏有哪些 HiDPI 档（不改任何设置）
DPTOUCH_SELFTEST_MENU=exit "$APP"

# 2) 真走一遍「显示缩放」菜单动作：切到 1280×853 再切回 1704×1136（会真的改屏幕）
DPTOUCH_SELFTEST_DISPLAY=exit DPTOUCH_NO_ALERT=1 "$APP"

# 3) 设置窗口: 打印整棵控件树 + 布局质检(越界/重叠/文字溢出) + 分步授权/首启引导自检
DPTOUCH_SELFTEST_WINDOW=exit DPTOUCH_NO_GUIDE=1 "$APP"
```

`DPTOUCH_NO_ALERT=1` 让所有弹窗只写日志（脚本/无头跑不会挂住）。

---

## 设备支持

已实测：

- **小米平板 9 Pro Max**（VID `0x2717` / PID `0x2D05`）—— 全部功能验证通过

默认只匹配上表里的设备，避免误碰小米其它产品。
如果你的小米平板也是 DP-in 且笔能移光标，打开菜单里的
**「允许未验证的小米设备」** 试试；能用的话欢迎提 issue 把型号加进白名单
（`src/dptouch_engine.py` 的 `KNOWN_DEVICES`）。

> **注意**：这个 app 只解决**笔**。手指触控能不能用，取决于平板固件是否对外接主机
> 上报触摸屏接口 —— 小米目前只在 Windows 上报（Windows 免驱可用，macOS 不行）。
> 详见 README 的「它解决什么问题」一节。

---

## 从源码构建

```bash
git clone https://github.com/InYvn/mipad-dp-touch.git && cd mipad-dp-touch
./build_app.sh          # 产出 dist-pyi/Mipad DP Touch.app
./make_dmg.sh           # 顺便打一个能直接发给别人的 DMG（含说明 + 拖放快捷方式）
```

构建脚本会自己准备环境、装依赖、打包、签名。
需要 Xcode 命令行工具（`xcode-select --install`）。

命令行版（无图形界面，适合脚本 / 排错）仍然可用：

```bash
python3 src/hid_bridge.py            # 看用法
python3 src/hid_bridge.py bridge --scroll
```

---

## 隐私

- 不联网，没有任何网络代码。
- 日志只写在 `~/Library/Logs/MipadDPTouch.log`，内容是你的笔事件计数 —— 不含坐标历史、不含输入内容。
- 权限只要「输入监控」（读笔）和「辅助功能」（发鼠标事件），两者都是这类工具的最低要求。

## 卸载

1. 菜单 →「退出」
2. 删掉 `/Applications/Mipad DP Touch.app`
3. 删掉 `~/Library/Application Support/MipadDPTouch/`
4. （可选）`系统设置 → 隐私与安全性` 里移除残留条目

## 许可

MIT
