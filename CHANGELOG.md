# 更新日志

## 1.1 — 2026-09-18

* **修好「停住不动 = 右键菜单」不好使** —— 笔尖的轻微抖动会把这一手误判成拖拽，抬手时判定就被跳过了；现在改用中位偏移判定，容差放大到 30px。
* **更新不用再去 GitHub 手动下** —— 菜单栏会自己查新版本（也可以手动点「检查更新…」）：查到后一键下载，按发版正文里的 SHA256 校验，退出后自动替换并重新打开；任何一步失败都回滚到旧版。只有查更新这一步会联网，「高级选项」里可以关掉。
* **认笔更稳** —— 不再只认某一个 VendorID：改成 VID 白名单 × 接口 PID × 笔接口 usage 三重匹配，插上就能认出来，不会时好时坏。
* **新增「光标基准屏」** —— 一块屏还是多块屏都行：菜单栏和设置里都能手动指定笔把光标推到哪块屏，默认仍跟着光标走。
* **任意手势都能绑「切换屏幕」** —— 比如把「停住不动」从右键菜单换成换屏：笔上按一下，光标基准屏换到下一块（按桌面从左到右的顺序遍历，跳过 Mac 自带屏，到头绕回第一块）。
* **修好拖拽失效**；「划选文字」并回「快速滑动」 —— 停住再划 = 拖窗口，快进快出划一段 = 划选文字，两手下笔时是哪种就整手走哪种。
* **设置窗口排版** —— 右上「恢复默认」不再贴着下面的卡片；顺带加了排版自检（控件越界 / 重叠 / 文字放不下都会在启动自检里报出来）。

## 1.0.0 — 2026-09-16

首个公开版本：**轻点 = 点击**、**快速划动 = 翻页滚动**、**停住再划 = 拖拽**、**停住不动 = 右键菜单**；HiDPI 档位切换（只改平板那块屏）；分步授权引导与连接状态显示。

下载与完整说明见 [Release v1.0.0](https://github.com/InYvn/mipad-dp-touch/releases/tag/v1.0.0)。
